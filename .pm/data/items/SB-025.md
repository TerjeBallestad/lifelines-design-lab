---
id: SB-025
type: issue
title: "[probe] Script editor — does live-compile text editing feel like the tool Terje reaches for?"
stage: done
priority: null
pillar: ""
related: []
affectedFiles: []
blockedBy:
  - SB-023
  - SB-024
aliases: []
sprintId: SPRINT-001
assignee: null
assignedAt: null
comments:
  - id: c1785945872236
    author: claude
    text: |-
      Probe BUILT — awaiting Terje's play session (the feel verdict is the resolution, so the ticket stays open).

      What exists: prototypes/script-editor/ (index.html + main.ts), served by the normal dev server. Run npm run dev, open http://127.0.0.1:5173/prototypes/script-editor/index.html. It loads the healed tiny-olsen.case.md and runs the REAL 0.2 compiler (src/compiler) on every keystroke (~120 ms debounce; compile itself is a few ms).

      Wired per mockup turn 5a: outline rail with per-kind counts and jump (documents + questions expanded) · editable script pane with line numbers, kind-colored headings, anchor/keyword paint, diagnostic line tints · follows-cursor preview (documents render as the letter with anchor highlights + THIS SECTION FEEDS chips; other nodes show their compiled output) · PROBLEMS list, click = jump · status bar: compiled · N nodes · ms, TODO count, stubs, warnings, quiet-day lint. ⌘S writes back to content/cases/olsen/tiny-olsen.case.md via a dev-only vite middleware (round-trip verified byte-identical). Canvas/Pacing/Playtest tabs are visible but dead on purpose.

      Not built (scope): ⌘K jump, script⇄canvas sync (turn 6), multi-line anchor paint. Verified: lint green, 196 tests pass incl. new jsdom smoke test (prototypes/script-editor/probe-smoke.test.ts), case:check green.

      THE QUESTION TO ANSWER WHILE PLAYING (≤5 min): edit real content — fix a TODO, add a fact anchor, break an id and watch the stub surface. Does this beat the IDE enough to keep building the editor direction? If script-first fails here, the whole editor direction (incl. SB-026 Canvas) re-opens.
    createdAt: 2026-08-05T16:04:32.236Z
  - id: c1785947674791
    author: claude
    text: |-
      Iteration 2 after Terje's first play (feedback: whole file overwhelming · Kind/Peek/Meta opaque · linking hard to reason about · highlighting too sparse · caret broken in cmux/Safari). Editor pane replaced with CodeMirror 6 (new devDeps):

      · Caret/alignment — native CM editing, no transparent-textarea overlay. Fixes the Safari caret outright.
      · Overwhelm — section folding (fold gutter ▸/▾), file opens fully folded = readable as an outline. Outline click unfolds and jumps. ⌘F search, undo history.
      · Field opacity — hover any field key (Kind:, Peek:, Meta:, when:, Slot:…) for a one-line doc pulled from docs/markup-0.2-reference.md. Hover any id: kind, its label/title, and the definition line — or 'stub, no definition yet'. Hover ~ verbs too.
      · Linking — autocomplete: typing fact: opens a fact picker with labels. Ids autocomplete in Supports:/Leads:/Opens:/Question: (kind-filtered), in when:/needs:/gate: conditions, in ~ effect lines, and in {…} weave guards. Typing a field key at line start completes keys with docs.
      · Sparse highlighting — every id colored by kind everywhere (doc_ blue, f_ accent, q_ yellow, h_ purple, t_ green, d_ orange, ck_ gold), plus «quotes», // comments, ~ effects, weave markers, {guards}, condition keywords, [icon=x], TODO lines. Compiler diagnostics render as CM underlines + tooltips.

      Verified: lint green, 196 tests (smoke test now edits through the CM view in jsdom), page serves. Same URL, reload.
    createdAt: 2026-08-05T16:34:34.791Z
  - id: c1785948267651
    author: claude
    text: "Iteration 3 (Terje: 'can I see the actual fact card / document?'): the preview rail now renders the real game surfaces, ported from blueprint_v1 paper/ink. Cursor in a ## fact block → the faktum card: domain glyph + domain·category tag, Caveat fact text, «quote», HENGER SAMMEN MED (supports → question titles, click jumps to the question block), KAN DRØFTES MED, KILDE (source doc found by anchor scan, click jumps — or the no-anchor lint warning). Cursor in document prose → the paper reader card with register styling (reg-notat lined paper, klinisk/formell Kalam) and yellow --mark anchors, click jumps to the fact. Kalam/Caveat/Architects Daughter via Google fonts. Lint green, smoke test green."
    createdAt: 2026-08-05T16:44:27.651Z
  - id: c1785949170157
    author: claude
    text: "Iteration 4 (Terje: 'the in-game versions look like this — I think they are baked'): confirmed and wired. Provenance: in-game docs are PNG textures in core-loop resources/cases/olsen/textures + UV manifest, baked by design-lab npm run bake:docs from per-kind HTML templates — which lived ONLY on branch sb-281-humane-case-authoring. Pulled templates/docs/ (kinds, registry, shared, fonts) onto main. Refactor for browser use: font embedding split into templates/docs/fonts-node.mjs; pageShell takes fontCss (bake passes base64 via fonts-node, browser passes vite ?url font-faces) — bake HTML output unchanged. Preview rail now renders the REAL templates in a scaled iframe (per-kind sheet sizes, BEKYMRINGSMELDING A4 / ØKONOMISK OVERSIKT ledger strip / MELDING lapp …), with runtime-style anchor highlights + click-to-jump (the bake keeps anchors neutral; the game overlays highlights — the editor plays that role). STATUSRAPPORT has no template → falls back to RAPPORT with a visible chip. Fact preview restyled to the in-game FUNN card (red rule, Fraunces quote, source line) with supports/kilde jump links below. Editor fonts = the actual bake TTFs. Verified: lint green, 197 tests (new test builds preview HTML for all 9 docs), serving OK."
    createdAt: 2026-08-05T16:59:30.157Z
  - id: c1785950060963
    author: claude
    text: "Iteration 5 (rail preview too small to read): click the page → lightbox over the editor at natural size (up to 86vw, 92vh scroll), Esc or backdrop closes. Anchor clicks inside the lightbox still jump to the fact block and close it. Rail keeps the fit-to-width thumbnail plus a hint line. Lint + tests green."
    createdAt: 2026-08-05T17:14:20.963Z
  - id: c1785951303197
    author: claude
    text: "INCIDENT + fix: Terje's in-editor modifications were lost. Cause: buffer-only edits + vite live-reload — my iteration commits during his session reloaded the open tab, and cmux suppresses the beforeunload prompt. Disk was untouched (mtime = my 18:03 round-trip test; file == git HEAD), so the edits were unrecoverable. Fix shipped: every edit persists to localStorage (300 ms debounce); a successful ⌘S clears the draft; on boot a draft that differs from disk is restored with 'restored unsaved draft — ⌘S to keep it' in the top bar. New regression test simulates reload + restore (198 total green). Session rule going forward: no code edits to the probe while Terje has a live editing session."
    createdAt: 2026-08-05T17:35:03.197Z
  - id: c1785952567053
    author: claude
    text: "Iteration 6 (line breaks lost in the doc HTML): root cause was compiler-side — emit.ts normalizeRunText collapses ALL whitespace, so paragraphs never reached labContent (the old generator had the same flattening; the baked in-game pages flow continuously too). Fix at the contract level, slice-safe: emit now records blank-line paragraphs per document and labContent emits one block per paragraph (ids doc_x_p1…); the slice's flat run list is untouched (core-loop canon stays whitespace-collapsed). render-doc.mjs + doc-preview.ts render every block as <p class=para>; shared BASE_CSS adds paragraph rhythm — so the next Terje-local bake:docs picks up paragraphs in the textures as well. entities.test.ts labContent expectation rewritten for the multi-block contract. Regenerated: tinyOlsen.ts + core-loop tiny_olsen_slice.json (this also flowed Terje's saved doc_bekymring rewrite downstream — verified the slice diff is exactly his content change, not the emit change). case:check green, 198 tests, lint green. NOTE for Terje: the rewrite dropped the f_aldri_alene anchor — new advisory: gate on call:grete references f_aldri_alene which nothing can pay."
    createdAt: 2026-08-05T17:56:07.053Z
  - id: c1785952941964
    author: claude
    text: "Iteration 7 (single line breaks inside a paragraph flattened): labContent runs now keep authored single newlines (new normalizeRunTextKeepBreaks in emit.ts, used only for the labContent block parse); the templates render \\n as <br> (shared.mjs renderInlineTokens). Slice canon verified untouched — the core JSON diff is still exactly Terje's content edit. New compiler test pins both sides (breaks kept in labContent, slice stays collapsed). 199 tests, case:check + lint green."
    createdAt: 2026-08-05T18:02:21.964Z
  - id: c1785953618306
    author: claude
    text: "Iteration 8: ⌘-click (ctrl-click) on any id jumps to its definition heading — CM domEventHandlers mousedown + the symbol table's defLine, section auto-unfolds via the existing jump path. While ⌘ is held, ids underline with a pointer cursor. Hover tooltip now says '⌘-click to jump'. Lint + tests green."
    createdAt: 2026-08-05T18:13:38.306Z
  - id: c1785954041873
    author: claude
    text: "Iteration 9: bake pipeline ported to main and RUN. scripts/blueprint/bake-docs.mjs pulled from sb-281, imports adapted to the 0.2 compiler CLI (buildTinyOlsenArtifacts + serializeSliceJson from compile-olsen-case.mjs — case-format.mjs's stableStringify was the identical tab serialization). npm scripts bake:docs / bake:docs:check restored. Chromium launches fine in this sandbox. Baked 8 docs + dagsrapport stationery + UV manifest into core-loop textures — Terje's bekymringsmelding rewrite AND the new paragraph/<br> rendering are now in the game textures. bake:docs:check green after bake. Note: doc_status (STATUSRAPPORT) is not in AUTHORED_DOC_IDS and has no template — it does not bake; tracked gap."
    createdAt: 2026-08-05T18:20:41.873Z
  - id: c1785954363552
    author: claude
    text: "RESOLUTION: script-first passes. Terje used the probe as the real tool — authored the bekymringsmelding rewrite in it, drove 9 iterations of feedback, and closed with 'we're done with this probe. All good'. The consolidated-studio bet holds: the .case.md is the tool; views are generated. Canvas (SB-026) inherits a live surface to bolt onto. Editor state at close: CodeMirror 6, live compile, folding outline, autocomplete, hover docs, goto-definition, real bake-template previews with lightbox, draft protection, ⌘S write-back, and the bake pipeline runs from main."
    createdAt: 2026-08-05T18:26:03.552Z
createdAt: 2026-08-05T12:40:17.802Z
updatedAt: 2026-08-05T18:26:19.094Z
doneAt: 2026-08-05T18:26:19.094Z
---
Question: the consolidated mockup bets on script-first (the .case.md IS the tool; views are generated). Does editing real Olsen content in a pane with live compile, inline stubs, TODO surfacing and the status bar actually beat editing the file in the IDE — enough to keep building?

Why it blocks: Canvas is a generated view OF this surface; if script-first fails the feel test, the whole editor direction re-opens.

Links: handoff mockup turns 5 (consolidated studio) + 6 (script⇄canvas sync) in 'Dialog Tree Editor.dc.html'; visual style = PM Board dark, mono ids, dots.

Probe shape: least HTML in prototypes/ wired to the real 0.2 compiler on the healed tiny-olsen.case.md. Terje plays.