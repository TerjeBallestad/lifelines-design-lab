---
id: SB-100
type: issue
title: "[research] Thought icon pipeline — one .aseprite source for editor and Godot"
stage: done
priority: null
pillar: ""
related: []
affectedFiles: []
blockedBy: []
aliases: []
sprintId: SPRINT-003
assignee: null
assignedAt: null
comments:
  - id: c1786615431961
    author: wayfinder
    text: |-
      FINDINGS (research fan-out, 2026-08-13). Icon reality today, both repos:

      **Godot renders textures, and the road for .aseprite icons already exists.** thought_bubble.gd resolves via ThoughtIcons.resolve(icon_key) -> `res://assets/sprites/icons/<key>.aseprite` if the file exists, else a code-drawn 32x32 placeholder from thought_bubble_icons.gd (~20 legacy keys: icon_hunger, icon_think, icon_book, ...; header says "TODO: Replace with proper pixel art atlas", and thought_icons.gd forbids new drawing functions). AsepriteWizard imports .aseprite natively (static-texture, PortableCompressedTexture2D, first_frame_only) — `load("res://...aseprite") as Texture2D` works. Exactly ONE real thought icon exists today: thought_doorbell.aseprite. Bubble chrome (bubble_cloud/bubble_dot.aseprite) and whole ui/handbok/mess/props sprite sets prove the pattern scales.

      **Icon key is the single shared identifier** across .sim.md (`Icon:` lines + ambient/* bullets ARE icon keys) -> *_sim_content.json -> generated .tres icon_key -> asset filename. Default when unauthored: icon_think (thought_line.gd).

      **Editor renders the key as a text chip, no image** (character-views.ts thoughtPreview, tv-icon span + fallback chip). Design-lab has NO public/ dir, no asset bundling for sprites; the only HTML-sprite precedent is a hand-exported base64 PNG (templates/docs/shared.mjs coin, manual re-export named in a comment). Baked docs must stay file://-openable.

      **Compile hand-off is text-only, one-directional** (compile-content.mjs writes JSON into ../lifelines-core-loop; --check mode must stay byte-deterministic; sibling-dir path is the only repo link). No aseprite CLI usage exists anywhere in either repo — the Wizard shells out to the Aseprite binary at Godot import time only.

      **Constraints for the pipeline decision (SB-102 / a design ruling):**
      1. Keep ThoughtIcons.resolve's `assets/sprites/icons/<key>.aseprite` convention and the .import files intact.
      2. The web side must address art by the same icon key.
      3. Partial migration must survive: Godot falls back to code-drawn, the editor already has a "no art yet" chip.
      4. 32x32 nearest-neighbour pixel art — HTML must render with image-rendering: pixelated.
      5. Must work in vite dev AND file:// baked docs.
      6. An export step (aseprite -b) adds the Aseprite binary as a gen:content toolchain dependency (today plain Node) — or exports get committed as PNGs beside the .aseprite sources.

      Full citation trail in the session transcript. Facts only — the pipeline pick happens on SB-102 or a dedicated ruling.
    createdAt: 2026-08-13T10:03:51.961Z
createdAt: 2026-08-13T09:57:02.137Z
updatedAt: 2026-08-13T10:03:52.136Z
doneAt: 2026-08-13T10:03:52.135Z
---
Gate requirement from Terje: thought icons must be .aseprite source files, editable, and rendered in BOTH the dialog editor (HTML) and Godot. The needle to thread: one icon source, two consumers, no manual export drift. Survey the options: aseprite CLI export on compile (gen:content step emits png/webp for the editor and the .tres path for Godot), a committed spritesheet + atlas, or per-icon exports. Check what the MTG ambient icon keys already reference in core-loop (AMBIENT_POOLS fold, thought_bubble_manager) — the icon set may partly exist. Deliverable: findings comment with a recommended pipeline; feeds the Thoughts-view task.

Links: SB-073 resolution, aseprite MCP tools exist in this environment.
