---
id: SB-031
type: issue
title: "[task] Markup patch layer — block spans to field-level text patches"
stage: done
priority: null
pillar: ""
related: []
affectedFiles:
  - src/compiler/parse.ts
  - prototypes/canvas
blockedBy: []
aliases: []
sprintId: SPRINT-001
assignee: null
assignedAt: null
comments:
  - id: c1785969167247
    author: wayfinder
    text: |-
      Resolved (cdfd0b1). src/compiler/patch.ts: pure ESM module over parseCaseText. API as sketched: patchField, listFieldAdd, listFieldRemove, appendBlock, removeBlock — each (text, …) → new text, reparse per call, single-line splices only.

      Acceptance, all proven in src/compiler/__tests__/patch.test.ts (21 tests, run against the real tiny-olsen file):
      (a) no-op patches are byte-identical (patchField same value, listFieldAdd of listed entry — incl. bracket-payload entries).
      (b) patch→compile equals the hand edit (byte-equal text, deep-equal slices).
      (c) list ops preserve line order and formatting (append to raw line, empty Needs: fill, payload-token remove, last-entry remove leaves bare key).
      (d) a six-patch chain composes and compiles with zero errors.

      Design notes for the canvas:
      - Field location mirrors parse.ts exactly: comment stripping, ' · ' composite splitting, FIELD_RE. A drift there mispatches — the two constants are duplicated with a keep-in-sync comment.
      - patchField inserts the field when absent (after the block's last field line). Repeated keys (Lead, Frank): first occurrence wins — per-index addressing is fog for later if the canvas needs it.
      - beat/conversation bodies are prose/weave, not fields: patchField throws PatchError there. Weave editing is SB-035's question.
      - listFieldRemove matches by exact entry or id token, so d_konto removes 'd_konto [type=…]' too. Reference cleanup (e.g. an event delta pointing at a removed clock) is the caller's concern, not the patch layer's.
      - appendBlock(fact) needs opts.documentId and lands at the end of that document's fact run. DEFAULT_TEMPLATES per kind are exported and overridable.
      - removeBlock(document) leaves its ## facts in place; the canvas composes removals for the whole family.
    createdAt: 2026-08-05T22:32:47.247Z
createdAt: 2026-08-05T22:20:24.281Z
updatedAt: 2026-08-05T22:32:54.695Z
doneAt: 2026-08-05T22:32:54.695Z
---
The work: a pure module over the compiler parse that turns canvas edits into minimal .case.md text patches. API sketch: patchField(text, blockId, field, value), listFieldAdd/Remove(text, blockId, field, entry) for Supports/Needs/Opens/Gate lines, appendBlock(text, kind, template), removeBlock(text, blockId). Uses the parser's block/field spans; never reformats untouched lines.

Why it blocks: every canvas edit verb (inspector forms, edge drag, node lifecycle) writes through this layer. DD-003: markup stays the store; the canvas edits it by patch.

Acceptance: unit tests prove (a) a no-op patch is byte-identical, (b) patch→compile equals editing the field by hand, (c) list ops preserve line order and formatting, (d) patches compose. Pure ESM like src/compiler — importable by node tests and Vite.