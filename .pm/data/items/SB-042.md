---
id: SB-042
type: issue
title: "[task] Canvas drag-wire-to-empty create — filtered menu, born wired, focused"
stage: done
priority: null
pillar: ""
related: []
affectedFiles: []
blockedBy: []
aliases: []
sprintId: SPRINT-001
assignee: null
assignedAt: null
comments:
  - id: c1786023209334
    author: claude
    text: |-
      D0 plan (wayfinder session, 2026-08-06). One task, canvas prototype only — no compiler changes needed (patchField inserts missing fields; every DEFAULT_TEMPLATES block carries the keys the wiring writes).

      Mechanism:
      1. Port drag released on empty viewport → floating create menu at the cursor. Kinds offered = kinds T where RELATION[from.kind→T] exists, plus document→fact (the ## block under the document IS the wire — the source edge is derived). Ports with no legal births (tiltak/dispatch/clock) show the refusal tip instead.
      2. Pick a kind → createNode gains opts.at: the drop point is pre-stored in the sticky POS store under the fresh id, so the node lands where released, not at the column end. Then connect(fromId, newId) writes the relation (skipped for document→fact). createNode selects, centers, and focuses the first inspector field — born wired, focused.
      3. The bare create dies: n/+ handler, z-new button, openCreatePanel removed. Document creation moves to the script lens (SB-040 ruling 2: script creates by naming). Duplicate/delete (SB-034) stay.

      Judges: compiler (npm run lint) + test (npx vitest run prototypes/canvas/probe-smoke.test.ts — new SB-042 describe block: legal menu, born-wired-at-drop-point, document→fact, refusal port, dead n key) + screenshot (dev server). Feel of the gesture → Terje at the next play session, not claimed here.
    createdAt: 2026-08-06T13:33:29.334Z
  - id: c1786023511101
    author: claude
    text: |-
      RESOLVED (wayfinder session, 2026-08-06). Built per the D0 plan comment — canvas prototype only.

      What landed:
      - Release a port drag on empty canvas → #create-menu at the cursor, offering only the kinds legal from that port: birthKinds() = RELATION[from→T] plus document→fact (the ## block under the document is the wire — source edge derived). Endpoint ports (tiltak/dispatch/clock) refuse with the cursor tip.
      - Pick a kind → dropCreate(): createNode gains opts.at (drop point pre-stored in the sticky POS store, so the node lands where released), then connect() writes the relation. Node is born wired with focus in its first inspector field. Exactly the F_NY failure mode (SB-036) this prevents: no node can be born unwired and off-screen from this gesture.
      - The bare create is dead: n/+ handler, z-new button, openCreatePanel removed. Document creation belongs to the script lens (SB-040 ruling 2). Duplicate/delete (SB-034) unchanged.

      Evidence:
      - compiler: npm run lint — tsc clean; prettier clean on all changed files (pre-existing unrelated warn on docs/sdd/blueprint-godot-port-spec.md, untouched, fails on main too).
      - test: npx vitest run prototypes/canvas/probe-smoke.test.ts — 19/19, incl. 5 new SB-042 tests (filtered menu, born-at-drop-point + wired + focused, document→fact, endpoint refusal, dead n key). Full suite 239/239.
      - screenshot: Playwright against the dev server — menu renders filtered from a fact port; after picking QUESTION, q_ny sits at the drop point, inspector shows FED BY supports f_grete_syk, Title field focused, save landed. The screenshot run's write to tiny-olsen.case.md was reverted.
      - terje rung not claimed: gesture feel goes to the next play session.
    createdAt: 2026-08-06T13:38:31.101Z
createdAt: 2026-08-06T13:26:35.674Z
updatedAt: 2026-08-06T13:38:47.931Z
doneAt: 2026-08-06T13:38:47.931Z
---
Replace the bare 'n → blank form' create (killed by SB-040 ruling 2). Drag from a port, release on empty canvas → create menu filtered by the RELATION legality table for that port → node lands wired, focus in its first inspector field. Duplicate/delete from SB-034 stay.

Why: the F_NY failure in SB-036 — a node born unwired and off-screen — is exactly what this gesture prevents.