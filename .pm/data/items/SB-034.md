---
id: SB-034
type: issue
title: "[task] Canvas node lifecycle — create, duplicate, delete, sticky layout"
stage: done
priority: null
pillar: ""
related: []
affectedFiles: []
blockedBy:
  - SB-031
aliases: []
sprintId: SPRINT-001
assignee: null
assignedAt: null
comments:
  - id: c1785970857485
    author: wayfinder-agent
    text: |-
      Resolved (48ee9f8). Create (n/+, per-kind panel, document picker for facts, fresh ids — dispatches minted d_-prefixed), duplicate (<id>_kopi from the source block's span), delete with a full inbound-reference scan shown BEFORE any write — each reference tagged ryddes/ryddes ikke; confirm cleans the patchable ones (list entries, top-level and-terms, hypothesis Question) and composes removeBlock calls, document deletes take their ## fact family. Sticky layout: positions per node id in localStorage (kildeverket-canvas-pos:<case>), stored positions win, only unplaced nodes get a column slot, 'nytt oppsett' recomputes barycenter once. Also fixed: fact→tiltak Needs edges now render (labContent as buildGraph option — the slice never exposes tiltak needs). 4 new tests, 234/234 green, tsc clean.

      Refuses to auto-clean (report-only, compiler stub diagnostics catch them): prose fact-anchors, effect lines, recipe pair headers, cond terms inside or/not/n-of.

      Fog for the map: (1) 'Needs' is kind-ambiguous — list on tiltak, condition on hypothesis; any generic code touching it will get this wrong. (2) No emit-side minimum-field gate exists — empty template blocks compile to visible nodes. (3) The position store is merge-only; a real editor wants pruning on delete.
    createdAt: 2026-08-05T23:00:57.485Z
createdAt: 2026-08-05T22:20:53.197Z
updatedAt: 2026-08-05T23:00:59.339Z
doneAt: 2026-08-05T23:00:59.338Z
---
The work: N / + creates a node of a chosen kind from a markup template block (appendBlock, SB-031), lands in its kind column, and opens the inspector for naming. Duplicate copies a block with a fresh id. Delete removes the block and warns about every inbound reference first (the compiler's ref map knows them).

STICKY LAYOUT: edits must not reshuffle the board — barycenter ordering runs once per load; new nodes append at the column end; positions persist per case (localStorage) until an explicit re-layout button.

Why it blocks: 'author new content' is a core verb of DD-003; layout jumps would make editing feel broken regardless.

Acceptance: jsdom tests for create/duplicate/delete round-trip through the compiler; delete of a referenced fact surfaces the reference list before writing; a create leaves every other node's position unchanged.