---
id: SB-078
type: issue
title: "[task] MobX spine — model.ts observable, autorun replaces the init-wiring callbacks"
stage: done
priority: null
pillar: ""
related: []
affectedFiles:
  - prototypes/canvas/model.ts
  - prototypes/canvas/main.ts
  - prototypes/canvas/editing.ts
blockedBy: []
aliases: []
sprintId: SPRINT-002
assignee: null
assignedAt: null
comments:
  - id: c1786283370785
    author: claude
    text: "Charting note (2026-08-09, after the SB-075 split): the four-ticket library adoption Terje chose — mobx spine (SB-078) → lit panels (SB-079, blocked by 078) → d3-force (SB-080) → d3-zoom/drag (SB-081, blocked by 080 for sequencing, not true dependency). Rationale thread: put library bones under the existing board instead of the React Flow rewrite; board SVG and sticky-layout semantics stay bespoke. The two d3 tickets are probes with Track F play rulings."
    createdAt: 2026-08-09T13:49:30.785Z
  - id: c1786284239438
    author: claude
    text: |-
      Resolved. model.ts is now the MobX spine: a shallow observable `state` (caseText, result/graph as refs, the derived index maps, crossJumpHeadings) plus an observable `ui` selection (selectedId, selectedEdgeKey) with setSelected/setSelectedEdge actions. The compiler stays pure — a compile swaps refs, nothing is deep-wrapped.

      Rewiring: rebuild() is one action fired by a reaction on state.caseText, so every committed buffer swap (field commit, edge write, lifecycle, lens typing) recompiles through one funnel and all reactions fire once over a consistent compile. Autoruns in main replace the callback bundles: highlight pass, inspector (renderDeleteConfirm wins while a delete is pending — that dissolved showDeleteConfirm/onDeleteCancelled), status bar, worklist, and a reaction for the canvas→script cross-jump (fires on selection CHANGE only, which replaces the old `changed` guard). editing.notes {save, lifecycle} and pendingDelete are observable, which dissolved onStatusChanged. EditingDeps is down to syncLens, clearLensDraftTimer, centerOn, focusFirstField, focusLensLineEnd; InspectorDeps to inspectorBody, showPreview, centerOn, showTip — the done-when bar.

      Probe-surface mirrors (an ES namespace cannot expose a getter): model.graph, main.selectedId/selectedEdgeKey/worklist, layout.layoutMode — each assigned in exactly one place, tests unchanged. Parity: 296/296 tests green (three consecutive full runs), tsc clean, probe-smoke.test.ts untouched.

      Note for the lit-panels ticket: panels should read model.state/model.ui directly (MobX reactive lit or autorun-driven re-render); the mirrors are for the smoke tests only — do not grow new consumers on them.
    createdAt: 2026-08-09T14:03:59.438Z
createdAt: 2026-08-09T13:48:56.997Z
updatedAt: 2026-08-09T14:03:59.913Z
doneAt: 2026-08-09T14:03:59.912Z
archivedAt: 2026-08-11T06:38:41.464Z
---
Make the lab state reactive. model.ts state (caseText, result, graph, selection, notes) becomes MobX observables; the render orchestration in rebuild() and the hand-rolled callback bundles (initEditing deps, onStatusChanged, onDeleteCancelled etc.) dissolve into autoruns.

Rules:
- The compiler stays pure. result/graph are observable.ref, never deep observables — deep-wrapping a compiled slice of hundreds of objects buys nothing.
- MobX is already in package.json — no new dependency.
- Pure rewiring: zero behavior change, the 288-test suite (probe-smoke.test.ts unchanged) is the parity bar.

Done when: the initEditing/initInspector dependency objects shrink to genuinely non-reactive concerns (lens handles, focus), and status bar / worklist / inspector re-render via autorun.